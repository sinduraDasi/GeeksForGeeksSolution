def binarysearch(self, arr, n, k):
		# code here
		left=0
		right=n-1
		mid=0
		while left<=right:
		    mid=(right+left)//2
		    if arr[mid] < k:
		        left=mid+1
		    elif arr[mid]>k:
		        right=mid-1
		    else:
		        return mid
		return -1
		        
